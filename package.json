{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Jacob Biehler",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": "8.16.0"
  },
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf build && cd ~/github.com/biehlerj/fullstackopen/part2/examples && yarn build --prod && cp -r build ~/github.com/biehlerj/notes-backend",
    "deploy": "git push heroku master",
    "deploy:full": "yarn build:ui && git add . && git commit -m uibuild && yarn deploy",
    "logs:prod": "heroku logs --tail"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.20.1",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.3",
    "morgan": "^1.9.1"
  }
}
